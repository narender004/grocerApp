/**
 * Styles
 * @format
 */

import { ScaledSheet, Sizing } from '@app/styles';

export const styles = ScaledSheet.create({
  swiperStyle: {
    height: '200@ms',
  },
  imageStyle: {
    height: '200@ms',
    width: Sizing.screen.width,
  },
  paginationStyle: {
    bottom: '10@ms',
  },
});
