/**
 * Cache Image Styles
 * @format
 */

import { ScaledSheet } from '@app/styles';

export const styles = ScaledSheet.create({
  placeholderImageStyle: {
    width: '100%',
    height: '100%',
  },
  loaderStyle: {
    position: 'absolute',
    alignSelf: 'center',
    top: '40%',
  },
});
