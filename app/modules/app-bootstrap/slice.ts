/**
 * App Bootstrap Slice
 * @format
 */

import { createAction } from '@reduxjs/toolkit';

// Actions )-------------------------------------
export const bootstrapApp = createAction('APP_BOOTSTRAP/BOOTSTRAP_APP');
